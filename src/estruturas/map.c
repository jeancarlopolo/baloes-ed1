#include "map.h"
#include "check.h"
#include "../formas/checkgeo.h"

Item copiaItem(Item item, Clausura c)
{
    if (c == NULL) // pro compilador não reclamar
        return item;
    return item;
}
